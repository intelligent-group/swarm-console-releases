# Swarm Console — Release Feed (Client Editions)

This repository hosts the auto-update feed for **client editions** of Swarm Console.
The app checks the latest published release here and offers a one-click download when
a newer build is available (Settings → **Update Swarm**, or the app menu → **Check for Updates**).

Internal editions use a separate feed and are not served from here.
